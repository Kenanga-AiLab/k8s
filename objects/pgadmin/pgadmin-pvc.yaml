kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: pgadmin-pvc
  labels:
    app: pgadmin
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  volumeName: pgadmin-pv
  volumeMode: Filesystem
