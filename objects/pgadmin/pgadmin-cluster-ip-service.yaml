apiVersion: v1
kind: Service
metadata:
  name: pgadmin-cluster-ip-service
  labels:
    app: pgadmin
spec:
  type: ClusterIP 
  selector:
    app: pgadmin
  ports:
    - port: 80
      targetPort: 80
      name: http